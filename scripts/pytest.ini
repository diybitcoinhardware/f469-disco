[pytest]
# Default: run unit tests only
# Integration tests in tests/integration/ require hardware
testpaths = disco_lib/tests/unit

# Run from scripts/ dir: pytest
# Run from repo root:    pytest scripts/disco_lib/tests/unit
